[project]
name = "normattiva"
version = "0.1.0"
description = "Assistente normativo per la PA italiana"
requires-python = ">=3.12"
dependencies = [
    "supabase>=2.0.0",
]

[tool.vercel]
entrypoint = "api.search:handler"
